import os
import pandas as pd


current_dir = os.path.dirname(os.path.abspath(__file__))
werk_folder_path = os.path.join(current_dir, "..", "Werk")
processes_folder_path = os.path.join(current_dir, "..", "Werk", "Prozesse")

class process_processes
